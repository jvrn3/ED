#ifndef MYSTR_H
#define MYSTR_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char *aloca_tamanho(char *str, size_t len);
char *arruma_path(char *str);
char *concatena(char *str, char *str2);

#endif
